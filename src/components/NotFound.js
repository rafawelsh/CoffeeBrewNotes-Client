import React from "react";
import { PageContainer } from "../styles/PageStyles";
function NotFound() {
	return (
		<PageContainer>
			<h1>Sorry, seems like we don't have that!</h1>
			<h2>Go back to brewing some coffee!</h2>
		</PageContainer>
	);
}

export default NotFound;
